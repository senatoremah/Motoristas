<%= form_for(@motoristum) do |f| %>
  <% if @motoristum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motoristum.errors.count, "error") %> prohibited this motoristum from being saved:</h2>

      <ul>
      <% @motoristum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control", placeholder: 'Informe o seu nome' %>
  </div>
  <div class="form-group">
    <%= f.label :cpf, class: 'control-label' %><br>
    <%= f.text_field :cpf, class: "form-control", placeholder: 'Informe o seu CPF' %>
  </div>
  <div class="form-group">
    <%= f.label :tipo_de_caminhao, class: 'control-label' %><br>
    <%= f.text_field :tipo_de_caminhao, class: "form-control", placeholder: 'Informe o tipo de caminhão' %>
  </div>
  <div class="form-group">
    <%= f.label :ano, class: 'control-label' %><br>
    <%= f.text_field :ano, class: "form-control", placeholder: 'Informe o ano do caminhão' %>
  </div>
  <div class="form-group">
    <%= f.label :marca, class: 'control-label' %><br>
    <%= f.text_field :marca, class: "form-control", placeholder: 'Informe a marca do caminhão' %>
  </div>
  <div class="actions">
     <%= f.submit "Cadastre-se", class: 'btn btn-success' %>
  </div>
<% end %>
